version: "3.3"

name: migrator.shrymp.de

services:
  app:
    build:
      context: .
    ports:
      - "8080:80"
    env_file:
      - .env
    container_name: shrymp-migrator
    restart: unless-stopped
